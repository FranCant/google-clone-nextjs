/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["www.google.co.uk","www.google.es","avatars.githubusercontent.com"],
  },
};

module.exports = nextConfig;
